# Session Commands

## start — Start a new diagram session

Starts an embedded server and opens a browser window with draw.io for real-time preview.

```bash
mcp2cli drawio session start
```

No parameters required.

Use `mcp2cli drawio session start --help` for full parameter details.
