# Span Commands

## get — Get span information and metrics

```bash
# Get span by ID
mcp2cli agentops span get --span-id xyz456abc789
```

Required: `--span-id`

Use `mcp2cli agentops span get --help` for full parameter details.
