# Graph Commands

## get — Read the entire knowledge graph

```bash
mcp2cli memory graph get
```

Returns all entities, their types, observations, and all relations in the knowledge graph.

Use `mcp2cli memory graph get --help` for full parameter details.
