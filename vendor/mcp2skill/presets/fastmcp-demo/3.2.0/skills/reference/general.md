# General Commands

## ping — Ping the server to check connectivity

```bash
mcp2cli fastmcp ping
```

No additional parameters supported.

Use `mcp2cli fastmcp ping --help` for full parameter details.
