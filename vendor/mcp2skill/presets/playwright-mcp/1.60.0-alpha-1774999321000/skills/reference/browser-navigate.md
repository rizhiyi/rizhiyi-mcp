# Browser Navigation Commands

## go — Navigate to a URL

```bash
mcp2cli playwright browser navigate go --url https://example.com
mcp2cli playwright browser navigate go --url https://github.com/login
```

## back — Go back to the previous page

```bash
mcp2cli playwright browser navigate back
```

Use `mcp2cli playwright browser navigate <cmd> --help` for full parameter details.
