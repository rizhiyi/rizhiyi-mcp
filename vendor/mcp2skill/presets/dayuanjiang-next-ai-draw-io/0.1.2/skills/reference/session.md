# Session Commands

## start — Start a new diagram session

```bash
mcp2cli session start
```

Opens a browser window with draw.io for real-time diagram preview. No parameters required.

Use `mcp2cli session start --help` for full parameter details.
